class AlertModel {
  final String title;
  final String description;
  final DateTime timestamp;
  AlertModel({
    required this.title,
    required this.description,
    required this.timestamp,
  });
}